# Default / private validation build
PUBLIC_SITE_PUBLIC=false

# Required only when HG-WEB-002 authorizes a public release.
# Example values are intentionally omitted: never invent production identity/contact data.
PUBLIC_SITE_URL=
PUBLIC_CONTACT_URL=
PUBLIC_CONTACT_LABEL=
